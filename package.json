{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "release": "lerna publish --no-private"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.2.0"
  }
}
